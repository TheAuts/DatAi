---
description: API security, .gitignore integrity, secrets management
alwaysApply: false
---

Enforce API security on all external calls. Never weaken `.gitignore`—data/, cache/, `.env`, and secrets stay untracked. Load credentials from environment variables only; never hardcode keys or commit secrets.
