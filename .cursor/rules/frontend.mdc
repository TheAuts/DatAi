---
description: Streamlit UI — DataFrame inputs, caching, responsive dark-mode layout
globs: "{dashboard.py,**/*streamlit*}"
alwaysApply: false
---

Use Streamlit. Keep UI data-agnostic—always accept DataFrames as input. Always use st.cache_data for heavy computations. Ensure responsive layouts and dark-mode styling.
